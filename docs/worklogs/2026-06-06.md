# 工作日志：2026-06-06

## 今日工作区快照

- 当前分支：`v1.2-agent-harness`
- GitHub 远端：`origin` -> `https://github.com/ehx0613/esg-multimodal-extraction-agent.git`
- 当前本地提交：`903b2cc` (`v1.1 token-safe stable baseline`)
- 本地分支与远端分支提交差异：领先 `0`，落后 `0`
- 今日尚无新 Git 提交，因此当前本地修改尚未上传到 GitHub。

> 注意：Git 无法可靠判断未提交修改具体完成于哪一天。以下内容是 2026-06-06 检查时的工作区快照。

## 当前修改概览

Git 状态中当前可见：

- `36` 个已跟踪文件被修改。
- `2` 个文档文件已暂存后又继续修改：`docs/architecture.md`、`docs/runbook.md`。
- `93` 个未跟踪文件或目录。
- `2226` 个已跟踪文件显示为删除，主要来自历史输出目录。

排除输出结果目录后，已跟踪代码和文档修改约为：

- `32` 个文件。
- `2578` 行新增。
- `1680` 行删除。

## 主要工作方向

根据当前文件变化，工作主要集中在：

- 扩展 Agent 架构，包括新增 `agents/supervisor_agent.py` 和 `pipeline/agent_harness.py`。
- 增加统一抽取、定量文本抽取和评级数据流程。
- 增加后端、前端及对应 API、数据库测试。
- 重构 ESG schema 配置，并增加行业适用性和可读 schema 文档。
- 改进 Route B / Route B2、混合检索、BM25、RRF、证据存储和结果校验。
- 改进 schema matching、字段引用、视觉补充抽取和 PDF ingest。
- 增加批处理、全量抽取、评级工作流、评估和结果检查脚本。
- 更新 README、架构文档、运行手册、演示指南和项目地图。

## GitHub 联动状态

仓库已经与 GitHub 联动，不需要重新添加远端。当前 GitHub 看不到本地工作，是因为修改还没有形成新提交，也没有执行推送。

常用流程：

```powershell
# 1. 查看修改
git status --short
git diff

# 2. 选择性暂存，避免误提交大量输出文件
git add <需要提交的文件或目录>

# 3. 检查即将提交的内容
git diff --cached --stat
git diff --cached

# 4. 创建本地提交
git commit -m "描述本次工作"

# 5. 上传当前分支到 GitHub
git push origin v1.2-agent-harness
```

## 上传前检查

- 确认 `2226` 个删除记录是否确实需要提交。
- 确认输出结果、备份目录和大文件是否应该通过 `.gitignore` 排除。
- 将代码、文档、生成结果拆成小而清晰的提交，便于审查和回退。
- 推送后，在 GitHub 上从 `v1.2-agent-harness` 分支创建 Pull Request。

